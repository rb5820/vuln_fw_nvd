<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- Main NVD Menu -->
    <menuitem 
        id="menu_nvd_root"
        name="NVD Vulnerability"
        sequence="50"
        web_icon="vuln_fw_nvd,static/description/icon.png"/>
    
    <!-- Configuration Menu -->
    <menuitem 
        id="menu_nvd_config"
        name="⚙️ Configuration"
        parent="menu_nvd_root"
        sequence="100"/>
    
    <!-- Operations Menu -->
    <menuitem 
        id="menu_nvd_operations"
        name="🔄 Operations"
        parent="menu_nvd_root"
        sequence="50"/>
    
    <!-- Reporting Menu -->
    <menuitem 
        id="menu_nvd_reporting"
        name="📊 Reporting"
        parent="menu_nvd_root"
        sequence="75"/>

    <!-- Webhook Actions -->
    <record id="action_nvd_connector" model="ir.actions.act_window">
        <field name="name">Connectors</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">vuln.fw.nvd.connector</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
    </record>

    <record id="action_nvd_webhook" model="ir.actions.act_window">
        <field name="name">Webhooks</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">vuln.fw.nvd.webhook</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
    </record>

    <record id="action_nvd_webhook_receiver" model="ir.actions.act_window">
        <field name="name">Webhook Receivers</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">vuln.fw.nvd.webhook.receiver</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
    </record>

    <record id="action_nvd_webhook_log" model="ir.actions.act_window">
        <field name="name">Webhook Logs</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">vuln.fw.nvd.webhook.log</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
    </record>

    <record id="action_nvd_webhook_payload_queue" model="ir.actions.act_window">
        <field name="name">Payload Queue</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">vuln.fw.nvd.webhook.payload.queue</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
    </record>

    <!-- Operations Menu Items -->
    <menuitem
        id="menu_nvd_connector"
        name="Connectors"
        parent="menu_nvd_operations"
        action="action_nvd_connector"
        sequence="10"/>

    <!-- Webhooks Menu -->
    <menuitem
        id="menu_nvd_webhooks"
        name="🔗 Webhooks"
        parent="menu_nvd_root"
        sequence="30"/>

    <menuitem
        id="menu_nvd_webhook"
        name="Outbound Webhooks"
        parent="menu_nvd_webhooks"
        action="action_nvd_webhook"
        sequence="10"/>

    <menuitem
        id="menu_nvd_webhook_receivers"
        name="Inbound Receivers"
        parent="menu_nvd_webhooks"
        action="action_nvd_webhook_receiver"
        sequence="20"/>

    <menuitem
        id="menu_nvd_webhook_logs"
        name="Webhook Logs"
        parent="menu_nvd_webhooks"
        action="action_nvd_webhook_log"
        sequence="30"/>

    <menuitem
        id="menu_nvd_webhook_payload_queue"
        name="📬 Payload Queue"
        parent="menu_nvd_webhooks"
        action="action_nvd_webhook_payload_queue"
        sequence="40"/>

</odoo>
